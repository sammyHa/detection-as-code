name: Validate Detections

on:
  pull_request:
    paths:
      - 'detections/**'
      - 'tools/**'
      - 'tests/**'
      - 'pyproject.toml'
      - '.github/workflows/validate.yml'
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  validate:
    name: Lint, validate, and unit test
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Validate Sigma rule syntax
        run: python tools/validate_sigma.py detections/

      - name: Run unit tests
        run: pytest tests/unit/ -v --tb=short

      - name: Lint Python tools
        run: |
          pip install ruff
          ruff check tools/ tests/

      - name: Conversion smoke test (Sigma → SPL + EQL)
        # Catches conversion regressions before they reach main.
        # Doesn't deploy — just confirms every rule converts cleanly.
        run: python tools/sigma_convert.py --source detections/ --output /tmp/build/

      - name: Detect logical-precedence bugs in converted SPL
        # pySigma sometimes drops parens around OR groups that have higher-precedence
        # AND siblings, which silently changes rule semantics in Splunk.
        # See docs/known_issues.md for context.
        run: python tools/spl_lint.py /tmp/build/splunk/

  summary:
    name: Pipeline summary
    runs-on: ubuntu-latest
    needs: validate
    if: always()
    steps:
      - name: Report
        run: |
          echo "### Detection-as-Code CI Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Validation: ${{ needs.validate.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Commit: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
